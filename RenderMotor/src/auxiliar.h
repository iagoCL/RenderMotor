#ifndef RENDER_MOTOR_HELPER_H
#define RENDER_MOTOR_HELPER_H

// Carga un fichero en una cadena de caracteres
char *loadStringFromFile(const char *fileName, unsigned int &fileLen);

#endif //RENDER_MOTOR_HELPER_H